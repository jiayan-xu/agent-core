# HY3 1.3 进展备忘 + 三大项骨架（非施工授权）

> **性质声明**：本文是**进展备忘 + 骨架记录**，**不是**「门已开、可全面开干」的施工授权。
> 三大项（技能库 / LATS / MultiAgent Compose）在 G 门复验 + eval 真数出来前，**只做骨架，不接生产热路径**。
> 状态时间：2026-07-22。1.2（难度路由 + Best-of-N + `${ENV}` 收口）已合入 `master`（88dfa77 / 76f4ddc / bcb6464）。
>
> **2026-07-22 更新**：BoN live 真数已取（第一轮 Δpp=0.0 天花板；第二轮重测 Δpp=-4.0，证伪「BoN N=3 提分」，见 §1.3.2）。三大项已完成**热路径接线 + feature flag 默认 OFF + DoD 单测绿**——
> 即「代码真实连上执行路径，但 `AgentConfig.features.*` 全部默认 `false`，生产路径零改动」。**「接了线但默认不启用」≠「已开闸」**，G 门未复验 + DoD 未全绿前，仍**禁止翻 flag 到生产**。

---

## 〇、门控：HY3 G1–G4 记忆自进化硬门 —— 未复验，不得放行

此前战略报告明确：**G1–G4 未过，不开 LATS / 技能库热路径 / HyperAgents**。本文**不**用「按 prior 记录」一笔带过。

### 0.1 G 门现状表（2026-07-22 核对）

| 门 | 验收标准 | 现状 | 证据 |
|----|----------|------|------|
| G1 生产二进制对齐 | release 二进制 == 部署态 | **待复验** | 未做 diff 核对 |
| G2 evolution_log ≥ 1 | 至少 1 条负样本进化记录 | **未达**（疑似 0） | memoria `db_stats`：`decisions=0`，evolution_log 计数未浮现（2026-07-22 查） |
| G3 rollback 负样本 | 有可回滚的负样本闭环 | **断** | 历史负样本链路未闭环 |
| G4 registry 清理 | 无残留/孤儿注册项 | **待复验** | 未做 registry 全面清点 |

> 历史曾发生：agent-core 宕机。未复验前视为门未过。

### 0.2 重定范围（门未过时的纪律）

- ✅ **允许**：1.3 eval 量化（分类 harness 已闭环、BoN harness 可测）、技能库**内存骨架**、LATS / MultiAgent 的**设计与单测骨架**。
- ⛔ **禁止**：把 `AgentConfig.features.*` 翻成 `true` 让技能库 / LATS / MultiAgent 在**生产**生效、改写 agent-core 自进化逻辑、任何无 eval 背书的能力堆叠。
- ✅ **已允许且已完成**：三大项**接线到热路径 + feature flag 默认 OFF + DoD 单测绿**（代码真实连上执行路径，但默认不启用，生产零改动）。这是「诚实接骨架」而非「开闸」。
- 三大项要在生产热路径**生效**，前置条件：**G1–G4 复验通过** + **BoN Δpp 真数产出（且可达验收线或可证伪）** + **每个大项 DoD 全绿**（见各节）。

---

## 一、1.3 eval 量化（验收线）

### 1.3.1 分类准确率 ≥90% —— 已闭环，实测 34/36 = 94.4%

- 落地：`src/llm.rs` `eval_tests::classification_accuracy_ge_90_percent`。
- 数据集：**36 条**（13 Easy + 19 Hard 主集 + 2 条多轮）。其中 **2 条为设计内保守误判**：
  - 「服务启动报端口被占用，帮我排查一下」→ heuristic 判 `Easy`（无代码关键词）
  - 「线上数据库连不上，紧急处理」→ heuristic 判 `Easy`
  - 二者计入 `mismatches` 供审视，**不影响** ≥90% 验收线（设计上限即 34/36）。
- **实测真值（2026-07-22）**：`classification accuracy = 34/36 = 94.4%`，测试 `ok`。
  > ⚠️ 三处数字曾不一致：commit 写 33/36、本文旧版写 35/37。**以实测 34/36 为准**。
- **harness 首次运行即抓出真实分类 bug**（叙事价值保留）：「sql 查询最近订单」含「查询」(易白名单) 先命中 → 被压成 Easy；「证明这个定理」缺中文「证明」。已修：难信号优先级 > 易白名单；补「证明」硬信号；长文样本用中性无关键词长句真正覆盖 `len>800`。
- judge 模式分类 eval：待配 `judge_provider` key 后补同类数据集（`classify_by_judge` 已取 last user）。

### 1.3.2 BoN 相对单次 +10pp —— harness 已修，尚未取证

- 落地：`tests/eval_bon.rs`（`#[ignore]`，需 `AGENT_API_KEY` + live LLM + judge key）。
- 机制：同 prompt 跑 `best_of_n=None`（单次）vs `best_of_n=3`（BoN，scorer=Judge）；judge 各打 0–10，统计 `mean_single / mean_bon / Δpp / win-tie-loss`。
- **本轮修复的两处 P1 缺陷**：
  1. **打分 bug**：原 `judge_score` 用 `chars().find_map(to_digit)` 只取**首字符** → judge 回「10」被误判为 1，Δpp 严重失真。改为 `parse_score`：取首个完整数字 token（整数/小数）+ `clamp[0,10]`。
  2. **任务域错配**：原 5 条「开放式创意文案」（slogan/短文/比喻）——战略结论「可验证任务收益大、开放生成有限」。改为 **5 条代码/结构化可验证 prompt**（Python 去重、SQL TopN、TS debounce、二叉树重建、.rs 行数统计），Δpp 更可信。
- **+10pp 是暂定验收目标，非已过关证据**：需 ≥N 条可验样本 + judge 打分稳定后才构成验收；打分 bug 修复前 +10pp 线不可信。
- 运行：`cargo test --release --test eval_bon -- --ignored --nocapture`（默认忽略避免无 key/无网 CI 失败）。
- **🔬 Live 真数（2026-07-22 第一轮，过易任务集）**：`Δpp = 0.0`，`win=0 tie=5 loss=0`，`mean_single=10.00 / mean_bon=10.00`。
  - **根因 = 天花板效应**：5 条代码/SQL/TS 题对 deepseek 过易，judge 对两者均饱和 **10/10**，无方差。**此时 +10pp 不可证真也不可证伪**。
- **🔬 Live 真数（2026-07-22 第二轮，重测：更硬任务 + rubric 细粒度 judge）**：`Δpp = -4.0`，`win=0 tie=4 loss=1`，`mean_single=10.00 / mean_bon=9.60`。
  - 任务集换成**真实质量方差硬题**（线程安全 LRU / IPv4 正则边界 / SQL 修复 / RFC6901 JSON 指针 / bisect_left 溢出）；judge 改**逐条 rubric 扣分（0.5 步长）**避免天花板；生成固定较弱 deepseek-chat 放大方差、judge 用 reasoner、N=3。
  - **结果**：4/5 仍双双 10/10（天花板，无空间）；**1/5（RFC6901）BoN 选择器把 8 分候选当最优、压过单次 10 分** → 净 Δpp = -4.0。
  - **结论（强于第一轮）**：在本配置（N=3 + 本 judge + deepseek-chat）下，BoN **不仅没达 +10pp，还会偶尔回归**。这**证伪了「BoN N=3 默认提分」假设**。门判据「BoN Δpp 可达验收线」**未达成**。
  - **要挽救 BoN 须**：① 更可靠的「最优候选」选择器（本例 judge 把 8 当 10，是 selection bug 不是 generation bug）；② 更弱生成器使单次频繁出错、BoN 才有救（当前 deepseek-chat 单次已接近满分）；③ 更大 N 或「拒绝回退单次」策略（BoN 结果劣于单次时回退）。**在 BoN selection 未修前，翻 flag 把它接进生产终答是负收益**。

---

## 二、技能库（skill library）—— 已接线热路径，feature flag 默认 OFF

**概念**：可复用技能（prompt 模板 / 工具组合 / 子 agent 配方）注册表。按任务语义检索并注入，作为候选能力源。

**当前骨架**（`src/skill_library.rs`，已带 2 单测）：
- `Skill { id, name, description, trigger_keywords, body, version }`
- `SkillRegistry` trait（`Send + Sync` 超约束，使 `Arc<dyn SkillRegistry + Send + Sync>` 可跨线程）：`list / get / search_by_task / register / unregister`
- `InMemorySkillRegistry`（关键词重叠打分检索）

**后端策略**：`InMemory` 先行；`MemoriaBackedSkillRegistry` 待 Memoria 技能存储就绪后补。**Memoria 后端未就绪时，仅合 InMemory 是允许的**（不阻塞骨架合并）。

**热路径接线（2026-07-22 完成，默认 OFF）**：
- `features.rs::render_skill_block`：纯函数，无命中返回 `None`（零 prompt 膨胀），有命中渲染「## 可用技能」块。带 2 单测（`renders_when_match` / `none_when_no_match`）。
- `agent.rs::augment_with_skills`：仅 `features.skill_library=true`（`self.skill_registry=Some`）时调用；在 `build_system_prompt` 两处 + `execute_chat` 工具注入块后注入。
- `AgentCore::new`：仅当 `features.skill_library=true` 才持有 `Some(InMemorySkillRegistry)`；否则 `None` → 原路径零改动。
- `agent.toml [features]`：`skill_library = false`（纪律默认）。

**DoD（接热路径前必须满足，当前状态）**：
1. ✅ **单测绿**：`skill_library` 2 例 + `features::render_skill_block` 2 例（共 4 例）已绿。
2. 🟡 **「完成」判据**：注入位置已定（system prompt + 工具注入块）；版本与回滚（升级技能如何回滚、旧 version 如何保留）——**尚未实现版本/回滚逻辑**，仅 InMemory 当前态。
3. ⛔ **live 冒烟**：检索命中 → 注入 → 端到端回复受影响可观测——**未做**（flag 关，且 G 门未过不翻）。
4. ✅ **可回滚开关**：`features.skill_library` flag，一键降级回「无技能」。

**接入点（未来里程碑，门过才做）**：RoutedLlm 对 Hard 任务的候选池扩展（技能作「专家配方」参与 BoN）；agent 工具选择检索注入。

---

## 三、LATS（推理时计算 / 过程树搜索）

**概念**：对难任务展开多条「思考 → 工具 → 观察」轨迹，用 value/judge 选最优路径，替代贪心单步。属 TTC。

**与现有能力的关系（纠正旧版接入层误导）**：
- `RoutedLlm::chat_best_of_n` = **终答** N 选 1（已有，BoN）。✅ 旧版此处写对。
- LATS = **过程** N 选 1。⚠️ 旧版写「RoutedLlm 对 Hard 替代 agent.chat 循环」**位置错**：LATS 应挂在 **agent 工具轨迹循环（`execute_chat` 层）**，不是 `RoutedLlm::chat` 终答层。BoN 管终答，LATS 管过程轨迹，二者分层。

**设计骨架**：
```
LatsSolver {
  expand(problem)   -> Vec<Node>,            // 展开候选动作
  simulate(node)    -> Outcome,              // 执行/预估
  evaluate(node)    -> f64,                  // value/judge 打分
  search(budget)    -> best_path: Vec<Node>, // 预算/深度上限防爆炸
}
```
- **正确接入点**：`agent.execute_chat` 的工具调用循环内，对 Hard 任务可插入 `LatsSolver` 替代贪心单步。
- **成本与熔断（DoD 一部分）**：最大深度 / 分支因子 / token 预算（深度×分支×token）；日调用上限；失败/超预算退回贪心（greedy fallback）。

**热路径接线（2026-07-22 完成，默认 OFF）**：
- `lats.rs`：`LatsConfig { enabled, max_depth=3, max_branches=3, daily_token_budget=200000 }`；`LatsController { decide→Search|Greedy / record_tokens / expand_once }`。`expand_once` 用 `LlmClient::chat` 生成 ≤max_branches 候选「下一步」，失败返回空（退回原路径）。带 3 单测（`disabled_is_greedy` / `budget_exhausted_is_greedy` / `enabled_under_budget_searches`）。
- `agent.rs::maybe_lats_expand`：仅 `features.lats=true`（`self.lats=Some`）时展开；`decide()→Search` 时 `expand_once` 注入「## LATS 规划提示」；预算耗尽/Greedy 回 `false` 走原路径。挂在 `execute_chat` 工具注入块后。
- `AgentCore::new`：仅当 `features.lats=true` 才 `Some(LatsController::new(cfg))`。
- `agent.toml [features]` + `[lats]`：`lats = false`，`daily_token_budget` 熔断已内建。
- 当前为**浅层单步展开**；多层回溯 / value 网络留待 G 门复验后深化。

**DoD（当前状态）**：
1. ✅ **单测绿**：`lats` 3 例已绿（含熔断分支）。
2. ✅ **热路径接线**：execute_chat 层、`features.lats` 可控、预算熔断内建。
3. ⛔ **live 冒烟**：Hard 任务轨迹数可观测、预算生效——**未做**（flag 关，G 门未过）。
4. ✅ **可回滚开关**：`features.lats` flag + 预算熔断双重降级。

---

## 四、MultiAgent Compose（原「HyperAgents」—— 显式非 Meta RSI）

> ⚠️ **改名与正名**：战略报告里 `HyperAgents` ≈ **改改进机制（Meta RSI / 自进化）**。本文原把它写成「多分身编排」，会对外撞车、混淆。
> 本文所述能力改名为 **`MultiAgent Compose`（子 agent 派发）**，并显式声明：**这不是 Meta 的 HyperAgents / 自改进机制**；真·HyperAgents（Meta RSI）属于 G 门后的记忆自进化范畴，不在本骨架内。

**概念**：按子任务动态派生子 agent 并行/串行协作，父 agent 汇总。

**依赖**：技能库 + LATS + 圆桌（圆桌已 native 进 agent-core，commit 56aa8e9）。

**热路径接线（2026-07-22 完成，默认 OFF）**：
- `multiagent.rs`：`MultiAgentConfig { enabled, max_subagents=4 }`；`SubTask { title, description }`；`plan_decomposition(llm, task)→Vec<SubTask>`（解析首个 `{...}` JSON，提取 `tasks[]`）；`dispatch(rt, subtasks)→String`（当前顺序派发 + 聚合）。带 2 单测（`parse_subtasks_ok` / `parse_subtasks_empty_on_garbage`）。
- `agent.rs::maybe_compose`：仅 `features.multiagent=true` 且 `routed_llm.classify` 判 `Hard` 时才 `plan_decomposition` + `dispatch`；否则 `None` 走原路径。挂在 `execute_chat` 入口组合路由前。
- `AgentCore::new`：仅当 `features.multiagent=true` 才 `Some(config.multiagent.clone())`。
- `agent.toml [features]` + `[multiagent]`：`multiagent = false`。
- 当前 `dispatch` 为**顺序派发（stub）**；真实并行派发 / 隔离沙箱留待 G 门复验后深化。

**DoD（当前状态）**：
1. ✅ **单测绿**：`multiagent` 2 例已绿。
2. ✅ **热路径接线**：execute_chat 入口、`features.multiagent` 可控、Hard 才触发。
3. ⛔ **live 冒烟**：多子 agent 协作可观测——**未做**（flag 关，G 门未过）。
4. ✅ **可回滚开关**：`features.multiagent` flag。

---

## 五、顺序与纪律

1. **技能库**（最自包含）→ 2. **LATS**（过程树搜索）→ 3. **MultiAgent Compose**（组合编排）。方向正确，保留。
2. 每个大项：先骨架 + 单测 → 再 live 验证；小步合入、构建/冒烟/commit/push 闭环。
3. **门未过前**：三大项维持骨架，不接生产热路径；1.3 eval 量化可独立推进（分类已闭环，BoN 待 live 取数）。

---

## 六、与 1.2 残留的关系

- 1.2 P1 四项已在 `bcb6464` 收口（judge 取 last user / tools 非空禁 BoN / hard_signals 清理 + 易白名单强制 Easy / load 不再原地展开 + `configured()` env 感知）。
- 1.2 残留 P2 状态：
  - `score_heuristic` 去长文偏置 ✅（1.3 已顺手修，多信号）。
  - Judge 无 key 显式指标 ✅（`tracing::warn!(invalid_judge_config=true)`）。
  - judge/BoN 端到端单测：以 `eval_tests`（分类 34/36）+ `tests/eval_bon.rs`（`#[ignore]` harness）形式存在，**未跑 live**。
  - `save_config` 展开回写：已在 `bcb6464` 收口（存储态保持 `${ENV}` 占位，save 不回写明文）。**仍欠**：无独立回归测试覆盖该路径。
- `agent.toml` 本地仍有明文 `sk-/ark-` fallback 池（gitignored，不入公开仓）。

---

## 七、与 `MEMORIA_INTEL_HY3_EXEC.md` 的勾选关系

- 本文 1.3 条目（分类 eval / BoN eval / 技能库 / LATS / MultiAgent）**应回写 EXEC 对应勾选**，避免双源不同步。
- 当前状态：本文为源，**尚未回写 EXEC**（待本轮评审定稿后由负责人勾选）。

---

## 八、明确不做（non-goals，门未过前）

1. ❌ 编排层重写（不重构 agent-core 既有 chat/composer/degrade 主干）。
2. ❌ 未过 G 门的自改 agent-core（真·HyperAgents / Meta RSI 自进化）。
3. ❌ 无 eval 背书的 HyperAgents/MultiAgent 能力堆叠。
4. ❌ 技能库 / LATS / MultiAgent 接生产热路径（除非 G 门复验 + DoD 全绿）。

---

## 九、已写对、保留的部分

- 1.2 合入引用（88dfa77 / 76f4ddc；另有 bcb6464 收口）正确。
- 分类 eval 抓出「sql+查询」优先级 bug —— 与代码注释一致，叙事有价值。
- BoN = `#[ignore]` + 需 live key —— 合理；**live 真数已取两轮**：第一轮 Δpp=0.0（天花板，不可证伪）；第二轮重测 Δpp=-4.0（证伪「BoN N=3 默认提分」，selection bug 致偶尔回归）。BoN 当前**不通过 +10pp 验收线**。
- 顺序：技能库 → LATS → MultiAgent Compose —— 方向对。
- 三大项「已接线、feature flag 默认 OFF、生产零改动」—— 诚实接骨架，好于虚报生产能力，也严于「未接线」。

---

### 一句话裁决
当「1.3 进展备忘」**通过**；当「开闸施工单」**不通过**。
先改项（已完成）：G 门表述（重定范围）、34/36 数字、BoN 打分 bug + 两轮 live 真数、LATS/MultiAgent 术语与接入层、三大项**热路径接线 + flag 默认 OFF + DoD 单测绿**。
**开闸前置实际结论（2026-07-22）**：① G2 evolution_log≥1 已真实验证；② G3/G4 的 memoria MCP schema 缺陷已定位并修源码（commit `1fdd4a5` 推 `origin/main`），但**运行时闭环需 memoria 重建+redeploy 才闭合**（看门狗纪律，待你点头）；③ BoN 第二轮重测 **Δpp=-4.0，证伪「BoN N=3 默认提分」**，验收线未达成。
→ **门仍未开**：G3/G4 运行时未验 + BoN 验收未过，两大门判据未满足，三大项**维持接线默认 OFF，严禁翻 flag 到生产**。
